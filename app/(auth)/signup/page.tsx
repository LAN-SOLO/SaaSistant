import { SignupForm } from "@/components/auth/signup-form"

export const metadata = {
  title: "Sign Up - SaaSistent",
  description: "Create your SaaSistent account",
}

export default function SignupPage() {
  return <SignupForm />
}
